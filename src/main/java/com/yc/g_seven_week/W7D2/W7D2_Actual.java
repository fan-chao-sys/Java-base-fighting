package com.yc.g_seven_week.W7D2;

public class W7D2_Actual {

    /**
     *  代码实战 ii：缓存 + 数据库一致性方案（先更新数据库，再删除缓存）
     *
     * 1. 方案选择
     *   推荐使用 先更新数据库，再删除缓存 方案（业界主流，兼顾性能与一致性）。
     * 2. 时序图（ASCII 版）
     *  客户端请求更新数据
     *     ↓
     *   1. 先更新数据库（UPDATE）
     *     ↓
     *   2. 删除 Redis 缓存（DEL key）
     *     ↓
     *  客户端查询数据时：
     *   3. 缓存未命中，从数据库读取最新数据
     *     ↓
     *   4. 写入 Redis 缓存（SET key value）
     *     ↓
     *  返回数据给客户端
     * 3. 异常场景处理
     *  数据库更新成功，缓存删除失败：用「重试机制 + 消息队列补偿」解决。
     *  并发读写问题：用「缓存过期时间 + 短暂互斥锁」兜底，极端情况数据短暂不一致，最终会被过期机制修正。
     *
     *
     *
     *
     *
     *
     */
}
